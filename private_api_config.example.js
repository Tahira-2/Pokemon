// TEMPLATE — copy to private_api_config.js. Leave GEMINI_API_KEY empty for
// BYO (every visitor pastes their own key). Only set it if you want to pay
// for everyone's Gemini calls and aren't deploying — this file is excluded
// from deploys regardless.
window.PRIVATE_CONFIG = {
    GEMINI_API_KEY: ''
};
